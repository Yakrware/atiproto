{
  "name": "atiproto",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "deploy": "bash scripts/deploy.sh"
  },
  "overrides": {
    "vite": "^7.1.7"
  }
}
